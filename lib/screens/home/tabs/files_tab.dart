import 'package:flutter/material.dart';

class FilesTab extends StatelessWidget {
  const FilesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('文件', style: TextStyle(fontSize: 24)),
    );
  }
}
