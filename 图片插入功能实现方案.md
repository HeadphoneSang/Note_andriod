# 图片插入功能实现方案

## 架构概览

```
┌──────────────────────┐       ① POST /file/upload         ┌──────────────┐
│  Flutter App         │ ──── multipart file ────→           │  Spring Boot │
│                      │                                     │              │
│  AppFlowy Editor     │ ←─── 返回图片 URL ────              │  FileController │
│  ImageBlockComponent │                                     │  MinioService │
│  (url: minio_url)    │                                     │              │
│                      │       ② POST /noteBlock/batchUpdate │              │
│  IncrementalSave     │ ──── image node 序列化为 deltaJson   │              │
│  (自动保存)          │ ←─── 加载时 Node.fromJson 还原      │              │
└──────────────────────┘                                     └──────┬───────┘
                                                                     │ ③ PUT
                                                                     ↓
                                                              ┌──────────────┐
                                                              │    MinIO     │
                                                              │  note-images │
                                                              │  bucket      │
                                                              └──────────────┘
```

---

## 实施顺序

### 第一阶段：后端（Spring Boot）

#### 1.1 添加 MinIO 依赖

**文件：** `pom.xml`

在 `pom.xml` 中添加 MinIO SDK 依赖：

```xml
<dependency>
    <groupId>io.minio</groupId>
    <artifactId>minio</artifactId>
    <version>8.5.17</version>
</dependency>
```

#### 1.2 添加 MinIO 配置

**文件：** `src/main/resources/application.yml`

```yaml
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: note-images
  public-url: http://47.104.25.40:9000/note-images
```

#### 1.3 创建 MinioService

**文件：** `src/main/java/com/xxx/note/service/MinioService.java`

```java
@Service
public class MinioService {

    @Value("${minio.endpoint}")
    private String endpoint;
    @Value("${minio.access-key}")
    private String accessKey;
    @Value("${minio.secret-key}")
    private String secretKey;
    @Value("${minio.bucket-name}")
    private String bucketName;
    @Value("${minio.public-url}")
    private String publicUrl;

    private MinioClient client;

    @PostConstruct
    public void init() {
        client = MinioClient.builder()
            .endpoint(endpoint)
            .credentials(accessKey, secretKey)
            .build();
        if (!client.bucketExists(
            BucketExistsArgs.builder().bucket(bucketName).build())) {
            client.makeBucket(
                MakeBucketArgs.builder().bucket(bucketName).build());
        }
    }

    public String upload(MultipartFile file, String objectName) {
        client.putObject(PutObjectArgs.builder()
            .bucket(bucketName)
            .object(objectName)
            .stream(file.getInputStream(), file.getSize(), -1)
            .contentType(file.getContentType())
            .build());
        return publicUrl + "/" + objectName;
    }
}
```

#### 1.4 创建文件上传 Controller

**文件：** `src/main/java/com/xxx/note/controller/FileController.java`

```java
@RestController
@RequestMapping("/file")
public class FileController {

    @Autowired
    private MinioService minioService;

    @PostMapping("/upload")
    public ResponseEntity<Map<String, Object>> upload(
        @RequestParam("file") MultipartFile file) {

        // 校验文件类型
        String contentType = file.getContentType();
        if (contentType == null || !contentType.startsWith("image/")) {
            return ResponseEntity.badRequest().body(Map.of(
                "code", 400, "message", "仅支持图片文件"
            ));
        }

        // 生成唯一文件名
        String originalName = file.getOriginalFilename();
        String ext = originalName != null
            ? originalName.substring(originalName.lastIndexOf("."))
            : ".jpg";
        String objectName = UUID.randomUUID() + ext;

        // 上传到 MinIO
        String url = minioService.upload(file, objectName);

        return ResponseEntity.ok(Map.of(
            "code", 200,
            "message", "ok",
            "data", Map.of("url", url)
        ));
    }
}
```

#### 1.5 验证后端

- 启动 Spring Boot 服务
- 用 Postman 测试 `POST /file/upload`，选择一张图片上传
- 确认返回 `{"code": 200, "data": {"url": "http://.../xxx.jpg"}}`
- 浏览器访问该 URL，确认图片可访问

---

### 第二阶段：Flutter 前端

#### 2.1 添加依赖

**文件：** `pubspec.yaml`

```yaml
dependencies:
  image_picker: ^1.0.0
```

执行 `flutter pub get`

#### 2.2 在 NoteDocumentConvert 中添加 image 类型映射

**文件：** `lib/core/editor/note_document_convert.dart`

修改 `_mapType` 方法（约第 131 行），在 `case 'divider'` 之后添加：

```dart
case 'image':
  return ImageBlockKeys.type;
```

修改 `_reverseMapType` 方法（约第 208 行），在 `case 'divider'` 之后添加：

```dart
case 'image':
  return 'image';
```

**注意：** 现在 `deltaJson` 以完整 Node JSON 格式保存（`node.toJson()`），`image` 类型由 `Node.fromJson` 自动还原，这里加映射是兜底兼容，确保未来如果后端按 `type` 字段做特殊处理时能正确识别。

#### 2.3 创建图片工具栏按钮

**文件：** `lib/screens/note/widgets/image_toolbar_item.dart`（新建）

```dart
import 'package:appflowy_editor/appflowy_editor.dart';
import 'package:flutter/material.dart';

ToolbarItem buildImageToolbarItem(VoidCallback onTap) {
  return ToolbarItem(
    id: 'image',
    tooltip: '插入图片',
    icon: (editorState, onHover) => IconBuilderWidget(
      icon: Icons.image_rounded,
      tooltip: '插入图片',
      onPressed: () => onTap(),
    ),
  );
}
```

#### 2.4 在 NoteEditor 中添加图片上传和插入逻辑

**文件：** `lib/screens/note/note_editor.dart`

**① 导入依赖：**

```dart
import 'package:image_picker/image_picker.dart';
import 'package:dio/dio.dart';
import 'package:note_for_android/screens/note/widgets/image_toolbar_item.dart';
```

**② 添加 `_pickAndInsertImage` 方法：**

```dart
Future<void> _pickAndInsertImage() async {
  final picker = ImagePicker();
  final XFile? image = await picker.pickImage(
    source: ImageSource.gallery,
    maxWidth: 1920,
    maxHeight: 1920,
    imageQuality: 85,
  );
  if (image == null) return;

  if (!mounted) return;
  showDialog(
    context: context,
    barrierDismissible: false,
    builder: (_) => const Center(child: CircularProgressIndicator()),
  );

  try {
    final formData = FormData.fromMap({
      'file': await MultipartFile.fromFile(image.path),
    });
    final response = await HttpClient.instance.upload<Map<String, dynamic>>(
      '/file/upload',
      formData: formData,
    );

    if (context.mounted) Navigator.pop(context);
    if (!mounted) return;

    if (response.isSuccess && response.data != null) {
      final url = response.data!['url'] as String;
      final selection = _editorState.selection;
      if (selection == null || !selection.isCollapsed) return;

      final node = _editorState.getNodeAtPath(selection.end.path);
      if (node == null) return;

      final transaction = _editorState.transaction;
      if (node.type == ParagraphBlockKeys.type &&
          (node.delta?.isEmpty ?? false)) {
        transaction
          ..insertNode(node.path, imageNode(url: url))
          ..deleteNode(node);
      } else {
        transaction.insertNode(node.path.next, imageNode(url: url));
      }
      transaction.afterSelection = Selection.collapsed(
        Position(path: node.path.next, offset: 0),
      );
      await _editorState.apply(transaction);
    } else {
      ToastUtil.error(context, title: "上传失败", description: response.message);
    }
  } catch (e) {
    if (context.mounted) Navigator.pop(context);
    if (context.mounted) {
      ToastUtil.error(context, title: "网络错误", description: "图片上传失败");
    }
  }
}
```

**③ 在工具栏 items 中添加图片按钮（约第 378 行）：**

```dart
MobileToolbar(
  editorState: _editorState,
  toolbarItems: [
    textDecorationMobileToolbarItem,
    headingMobileToolbarItem,
    blocksMobileToolbarItem,
    listMobileToolbarItem,
    todoListMobileToolbarItem,
    codeMobileToolbarItem,
    quoteMobileToolbarItem,
    dividerMobileToolbarItem,
    linkMobileToolbarItem,
    _tableInsertItem,
    _tableActionItem,
    buildTextAndBackgroundColorMobileToolbarItem(),
    buildImageToolbarItem(_pickAndInsertImage),  // ← 新增
  ],
),
```

#### 2.5 在 NoteViewPage 中添加图片上传和插入逻辑

**文件：** `lib/screens/note/note_view_page.dart`

完全相同的修改：导入、添加 `_pickAndInsertImage` 方法、在工具栏 items 中添加图片按钮。

---

### 第三阶段：验证

#### 3.1 功能验证

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开编辑笔记页面 | 工具栏底部出现图片按钮 |
| 2 | 点击图片按钮 | 弹出系统相册选择器 |
| 3 | 选择一张图片 | 显示加载中，图片上传到 MinIO |
| 4 | 上传完成 | 图片显示在编辑器中光标位置 |
| 5 | 拖拽图片边缘 | 图片宽度可调整 |
| 6 | 保存笔记 | 保存成功，无报错 |
| 7 | 关闭页面，重新打开笔记 | 图片正常加载显示 |
| 8 | 刷新页面 | 图片正常加载，无异常 |

#### 3.2 异常场景验证

| 场景 | 操作 | 预期结果 |
|------|------|----------|
| 网络断开 | 选择图片后断网 | 提示"网络错误，图片上传失败" |
| 选择非图片文件 | 通过文件管理器选择非图片文件 | 由 `image_picker` 自动过滤，不会出现 |
| 大图片 | 选择 10MB+ 图片 | 自动压缩到 1920px、85% quality，上传成功 |
| 后端返回错误 | 后端异常 | 提示"上传失败"+ 后端错误信息 |

---

## 文件变更汇总

| 文件 | 操作 | 阶段 |
|------|------|------|
| `pom.xml` | 修改 | 一 |
| `application.yml` | 修改 | 一 |
| `MinioService.java` | 新建 | 一 |
| `FileController.java` | 新建 | 一 |
| `pubspec.yaml` | 修改 | 二 |
| `lib/core/editor/note_document_convert.dart` | 修改 | 二 |
| `lib/screens/note/widgets/image_toolbar_item.dart` | 新建 | 二 |
| `lib/screens/note/note_editor.dart` | 修改 | 二 |
| `lib/screens/note/note_view_page.dart` | 修改 | 二 |