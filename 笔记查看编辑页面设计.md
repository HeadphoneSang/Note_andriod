# 笔记查看编辑页面设计

## 1. Context

当前 App 只有"新建笔记"的 `NoteEditor`（[note_editor.dart](lib/screens/note/note_editor.dart)），列表点击笔记只 `debugPrint` 不导航。需要新增一个**查看编辑已有笔记**的独立页面：列表点击进入 → 打开即编辑 → 支持删除/收藏/切换笔记本。

已确认的产品决策：
- **打开即编辑**（合一模式，不分离只读/编辑两个状态）
- **新建独立页面**（不复用 NoteEditor 本身，但复用其全部服务与子组件）
- **操作**：删除、收藏/星标、切换笔记本（不需要分享/导出）

---

## 2. 页面概览

新建独立页面 `lib/screens/note/note_view_page.dart`：

```dart
class NoteViewPage extends StatefulWidget {
  final Note note;           // 必填 — 完整 Note 对象（列表传入，含 id/title/summary/version/tags）
  final int? notebookId;     // 可选 — 来源笔记本（列表过滤用）
}
```

> 传入完整 `Note` 而非 `noteId`：列表接口 `/note/list`、`/note/notes` 返回的就是完整 Note 对象，直接透传即可，同时满足服务构造对 noteId 和 currentNoteInfo 的需求。

### 复用的现有组件

| 组件 | 来源 | 用途 |
|------|------|------|
| `IncrementalSaveService` | [mixins/incremental_save.dart](lib/screens/note/mixins/incremental_save.dart) | 增量保存正文/title/摘要 |
| `TagService` | [mixins/tag_service.dart](lib/screens/note/mixins/tag_service.dart) | 标签管理 |
| `showNotebookSheet` / `SelectNotebookButton` | [widgets/notebook_sheet.dart](lib/screens/note/widgets/notebook_sheet.dart) | 切换笔记本 |
| `showNoteInfoSheet` | [widgets/note_info_sheet.dart](lib/screens/note/widgets/note_info_sheet.dart) | 摘要 + 标签信息栏 |
| `SaveStatusOverlay` | [widgets/save_status_overlay.dart](lib/screens/note/widgets/save_status_overlay.dart) | 保存动画 + 最近保存时间 |
| `EditorFloatingToolbar` | [widgets/floating_toolbar.dart](lib/screens/note/widgets/floating_toolbar.dart) | 选中文本浮动菜单 |
| `MobileToolbar` + toolbar items | [widgets/table_toolbar_items.dart](lib/screens/note/widgets/table_toolbar_items.dart) | 编辑工具栏 |

### 复用点验证（代码调研结论）

- `IncrementalSaveService._flushPending` 在 `_noteId != null` 时**自动跳过创建步骤**，直接走 title/blocks 更新逻辑 — 编辑已有笔记开箱即用
- `tryLoadChunks()` 把服务端 blocks 灌入编辑器，并在内部完成 `_resetNodeMeta` + `_resetPending`，保证后续增量 diff 版本正确
- `TagService.tryLoadTags()` 加载笔记标签

---

## 3. 前置改造：IncrementalSaveService 支持预设 Note

[incremental_save.dart](lib/screens/note/mixins/incremental_save.dart) 是唯一必须改动的前端服务。

**为什么要传整个 Note 而不是只传 noteId：**

服务里 `_currentNoteInfo` 被多处直接依赖：

| 方法 | 依赖 | 行号 |
|------|------|------|
| `_tryUpdateTitle` | `_currentNoteInfo!.id` / `.version` | [incremental_save.dart:661-663](lib/screens/note/mixins/incremental_save.dart#L661-L663) |
| `updateSummary` | `_currentNoteInfo == null` 判空、比较 `summary` | :702-703 |
| `_tryUpdateSummary` | `_currentNoteInfo!.id` / `.version` | :712-714 |

只传 noteId 的话，`_currentNoteInfo` 仍是 null，上述方法一调用就 null 断言崩溃。列表返回的 `Note` 对象已含全部所需字段，直接作为 `existingNote` 传入最自然。

改造：

```dart
// 构造函数新增可选参数
IncrementalSaveService({
  ...,
  Note? existingNote,   // 新增：编辑已有笔记时传入完整 Note
}) {
  _noteId = existingNote?.id;        // 预设 noteId
  _currentNoteInfo = existingNote;   // 预设 Note 元信息（id/version/summary 立即可用）
  ...
}
```

- 不传时 `_noteId = null`、`_currentNoteInfo = null`，保持新建流程不变（首次 flush 走 `_tryCreateNote`）
- 传入时 `_noteId != null`，`_flushPending` 自动跳过创建步骤走 title/blocks 更新
- `_currentNoteInfo` 立即有值，`_tryUpdateTitle` / `updateSummary` / `_tryUpdateSummary` 开箱即用
- **无其他改动** — 不需要改 `_flushPending` 本身

---

## 4. 页面结构

### AppBar

| 位置 | 内容 |
|------|------|
| 左侧 | 返回按钮（返回前自动 `flush()` 保存未提交修改） |
| 标题 | 笔记标题（实时编辑，防抖自动保存） |
| 右侧 | 星标按钮（切换 isMarked）、更多菜单（删除 / 切换笔记本） |

### 主体（与 NoteEditor 布局一致）

1. 标题 TextField（复用 NoteEditor 样式：无边框、22px、w600）
2. 信息栏：创建时间、总字数、当前笔记本（可点击切换）
3. `MobileToolbar` + `AppFlowyEditor`
4. `SaveStatusOverlay`（右下角保存动画 + 最近保存时间）
5. `EditorFloatingToolbar`（选中文本时显示）

---

## 5. 初始化流程（打开即编辑）

```
initState:
  _editorState = EditorState.blank(withInitialText: false)
  _saveService = IncrementalSaveService(
    editorState, titleCtrl,
    provideNotebookId, provideContext,
    notebookId: widget.notebookId,
    existingNote: widget.note,            // ← 新增参数：传完整 Note
  )
  _tagService = TagService(noteIdProvider: () => _saveService.noteId)
  订阅 transactionStream → _saveService.dispatch(op)

postFrame:
  显示加载遮罩
  _titleCtrl.text = widget.note.title       // 标题直接用传入 Note 预填
  await _saveService.tryLoadChunks()        // blocks → editor（关键步骤）
  await _tagService.tryLoadTags()
  await _saveService.tryLoadAllNotebooks()  // 笔记本列表
  关闭遮罩
```

要点：
- 加载完成前保持全屏遮罩，避免用户看到空文档
- **`tryLoadNote()` 不再必须**：`_currentNoteInfo` 已由构造函数预设（含 version），直接进入 `tryLoadChunks` 即可编辑保存。若担心列表返回的 version 不是最新，可保留一次 `tryLoadNote()` 刷新最新版本号
- 顺序固定：`tryLoadNote`（可选）→ `tryLoadChunks` → `tryLoadTags` → `tryLoadAllNotebooks`
- 失败任一环节给出 toast 提示，但不阻塞整体

---

## 6. 三个操作功能

### ① 收藏/星标

- AppBar 星标 `IconButton`，点击切换 `isMarked`（乐观更新 UI）
- 调用后端 `POST /note/mark`（参数 noteId、isMarked、version）
- 409 冲突时提示"有用户修改了内容，请刷新后重试"（参考 `_tryUpdateTitle` 模式）
- 列表卡片已有星标显示（[note_list.dart](lib/screens/home/tabs/widgets/note_list.dart) line 217），返回后自动刷新

### ② 删除笔记

- 更多菜单 → "删除" → 确认弹窗（AlertDialog，参考 [notebook_sheet.dart](lib/screens/note/widgets/notebook_sheet.dart) 确认弹窗模式）
- 确认后调用后端 `POST /note/delete`（参数 noteId, version）
- 成功 → `Navigator.pop(context, true)` 返回列表，列表收到结果后刷新

### ③ 切换笔记本

- 直接复用 `showNotebookSheet`（已支持选择 + 新建笔记本）
- 切换成功后更新本地 `_selectedNotebookId`
- 注意：返回列表后，若按笔记本过滤，该笔记会从当前列表消失

---

## 7. 后端新增接口

| 接口 | 参数 | 说明 |
|------|------|------|
| `POST /note/mark` | noteId, isMarked, version | 切换收藏。成功返回完整 Note JSON（与 `/note/getNote` 一致）；409 版本冲突 |
| `POST /note/delete` | noteId, version | 软删除（置 isDelete/deletedAt）。成功 200 |

参考现有接口风格：`/note/title`（POST + queryParameters + 409 冲突 + 成功返回完整 Note）。

---

## 8. 列表接入

[notes_tab.dart](lib/screens/home/tabs/notes_tab.dart)：

```dart
// 当前 onNoteTap 仅 debugPrint（约 line 470）→ 改为导航
onNoteTap: (note) {
  Navigator.push<bool>(
    context,
    PageRouteBuilder(
      pageBuilder: (_, _, _) => NoteViewPage(
        note: note,                          // 传入完整 Note 对象
        notebookId: _selectedNotebookId,
      ),
      // 复用 _openAddNoteDialog 的 slide-up 转场
    ),
  ).then((deleted) {
    if (deleted == true || !mounted) return;
    setState(() => _noteListRefreshKey++);   // 强制 NoteList 重建刷新
    _loadNotebooks();
  });
},
```

- 返回结果传 `true` 表示"已删除"（不需要再刷新，或也需要刷新以移除列表项）
- 与 `_openAddNoteDialog` 的返回刷新逻辑保持一致

---

## 9. 实施顺序

1. `IncrementalSaveService` 构造函数加 `Note? existingNote` 参数（预设 `_noteId` + `_currentNoteInfo`）
2. 新建 `note_view_page.dart`（页面骨架 + 初始化加载 + 星标/删除/切换笔记本）
3. `notes_tab.dart` 接 onNoteTap 导航
4. （后端）新增 `/note/mark`、`/note/delete` 接口

---

## 10. 验证

1. 列表点击笔记 → 进入编辑页，标题/内容/标签正确加载
2. 修改内容 → 防抖 3s 后自动保存 → 返回列表 → 重新进入 → 修改已持久化
3. 星标切换 → 返回列表 → 卡片显示星标
4. 切换笔记本 → 成功 → 返回列表该笔记出现在新笔记本
5. 删除 → 确认弹窗 → 返回列表该笔记消失
6. `flutter analyze` 无 error
